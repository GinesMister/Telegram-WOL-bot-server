{
    // Language that will be used in bot messages. Possible values: 'es' or 'en'.
    languageMessages: '',

    // Devices you want to wake up.
    devices: [
        {
            macAddress: '',
            // Device name (must be unique).
            nameId: '',
            // Telegram usernames allowed to wake this device. Starts with "@".
            telegramUsernamesAuthorizedToWake: [''],
        },
    ]
}